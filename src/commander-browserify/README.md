# commander-browserify

from [Commander.js](https://github.com/tj/commander.js),Can be run cli in the browser

> :warning:
> don't support Stand-alone executable (sub)commands
> parse parseOptions.from only support 'user'
